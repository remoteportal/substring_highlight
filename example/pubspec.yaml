name: example
description: substring_highlight example

version: 1.0.3

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  substring_highlight:
    path: ../

flutter:
  uses-material-design: true
